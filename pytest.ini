[pytest]
testpaths = tests
pythonpath = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
addopts = -q --tb=short
markers =
    unit: pure unit tests, no external dependencies or mocks
    integration: tests requiring mocks, LangGraph, or external services
